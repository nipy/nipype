language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install:
  - pip install numpy
  - pip install scipy
  - pip install nibabel
  - pip install networkx
  - pip install traits
# command to run tests
script: make test