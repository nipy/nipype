# AUTO-GENERATED by tools/checkspecs.py - DO NOT EDIT
from nipype.testing import assert_equal
from nipype.interfaces.camino2trackvis.convert import Camino2Trackvis
def test_Camino2Trackvis_inputs():
    input_map = dict(ignore_exception=dict(nohash=True,
    usedefault=True,
    ),
    min_length=dict(units='mm',
    position=3,
    argstr='-l %d',
    ),
    data_dims=dict(mandatory=True,
    sep=',',
    position=4,
    argstr='-d %s',
    ),
    out_file=dict(position=2,
    genfile=True,
    argstr='-o %s',
    ),
    args=dict(argstr='%s',
    ),
    voxel_order=dict(position=6,
    mandatory=True,
    argstr='--voxel-order %s',
    ),
    terminal_output=dict(mandatory=True,
    nohash=True,
    ),
    environ=dict(nohash=True,
    usedefault=True,
    ),
    in_file=dict(position=1,
    mandatory=True,
    argstr='-i %s',
    ),
    voxel_dims=dict(mandatory=True,
    sep=',',
    position=5,
    argstr='-x %s',
    ),
    nifti_file=dict(position=7,
    argstr='--nifti %s',
    ),
    )
    inputs = Camino2Trackvis.input_spec()

    for key, metadata in input_map.items():
        for metakey, value in metadata.items():
            yield assert_equal, getattr(inputs.traits()[key], metakey), value
def test_Camino2Trackvis_outputs():
    output_map = dict(trackvis=dict(),
    )
    outputs = Camino2Trackvis.output_spec()

    for key, metadata in output_map.items():
        for metakey, value in metadata.items():
            yield assert_equal, getattr(outputs.traits()[key], metakey), value
